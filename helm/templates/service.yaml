apiVersion: v1
kind: Service
metadata:
  name: {{ include "hn-crawler.fullname" . }}
  labels:
    {{- include "hn-crawler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hn-crawler.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
