apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "hn-crawler.fullname" . }}
  labels:
    {{- include "hn-crawler.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
